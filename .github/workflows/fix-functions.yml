name: Fix App Functions

on:
  push:
    branches: [main]

jobs:
  fix:
    if: "${{ !startsWith(github.event.head_commit.message, 'fix: app functions') }}"
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - name: Patch App.jsx
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('src/App.jsx')
          s = p.read_text()
          s = s.replace('  ChevronRight,\n', '  ChevronRight,\n  Trash2,\n')
          s = s.replace("  const [payments, setPayments] = useState([]);\n", "  const [payments, setPayments] = useState([]);\n  const [saving, setSaving] = useState(false);\n")
          old = """  const save = async () => {\n    if (!amount || Number(amount) <= 0) return Alert.alert('Invalid amount', 'Please enter a valid amount.');\n    if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(date)) return Alert.alert('Invalid date', 'Use YYYY-MM-DD format.');\n    await addTransaction({ type, amount: Number(amount), categoryId, paymentMethodId, date: new Date(`${date}T12:00:00`).getTime(), note: note.trim() });\n    Alert.alert('Saved', 'Transaction saved successfully.');\n    navigation.goBack();\n  };"""
          new = """  const save = async () => {\n    if (saving) return;\n    if (!amount || Number(amount) <= 0) return Alert.alert('Invalid amount', 'Please enter a valid amount.');\n    if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(date)) return Alert.alert('Invalid date', 'Use YYYY-MM-DD format.');\n    if (!categoryId) return Alert.alert('Select category', 'Please choose a category.');\n    if (!paymentMethodId) return Alert.alert('Select payment method', 'Please choose a payment method.');\n    try {\n      setSaving(true);\n      await addTransaction({ type, amount: Number(amount), categoryId, paymentMethodId, date: new Date(`${date}T12:00:00`).getTime(), note: note.trim() });\n      Alert.alert('Saved', 'Transaction saved successfully.', [{ text: 'OK', onPress: () => navigation.goBack() }]);\n    } catch (error) {\n      Alert.alert('Save failed', 'Could not save the transaction. Please try again.');\n    } finally {\n      setSaving(false);\n    }\n  };"""
          s = s.replace(old, new)
          s = s.replace('<Pressable style={styles.primaryBtn} onPress={save}><Text style={styles.primaryText}>Save Transaction</Text></Pressable>', '<Pressable disabled={saving} style={[styles.primaryBtn, saving && styles.disabledBtn]} onPress={save}>{saving ? <ActivityIndicator color="#fff" /> : <Text style={styles.primaryText}>Save Transaction</Text>}</Pressable>')
          old = """  const [loading, setLoading] = useState(true);\n  const load = async () => { setLoading(true); setTxs((await getTransactions()).sort((a, b) => b.date - a.date)); setLoading(false); };"""
          new = """  const [loading, setLoading] = useState(true);\n  const load = async () => {\n    try {\n      setLoading(true);\n      const data = await getTransactions();\n      setTxs([...data].sort((a, b) => b.date - a.date));\n    } catch (error) {\n      Alert.alert('Load failed', 'Could not load transactions. Please try again.');\n    } finally {\n      setLoading(false);\n    }\n  };"""
          s = s.replace(old, new)
          s = s.replace('<SlidersHorizontal size={21} color={colors.muted} />', "<Pressable onPress={() => Alert.alert('Filter transactions', 'Choose a transaction type.', [{ text: 'All', onPress: () => setFilter('all') }, { text: 'Expense', onPress: () => setFilter('expense') }, { text: 'Income', onPress: () => setFilter('income') }, { text: 'Cancel', style: 'cancel' }])} style={styles.iconButton}><SlidersHorizontal size={21} color={colors.muted} /></Pressable>")
          s = s.replace("renderItem={({ item }) => <Pressable onLongPress={() => remove(item.id)}><TransactionRow tx={item} /></Pressable>}", "renderItem={({ item }) => <TransactionRow tx={item} onDelete={() => remove(item.id)} />}")
          s = s.replace('<Text style={styles.hint}>Long press a transaction to delete it.</Text>', '<Text style={styles.hint}>Use the trash button on a transaction to delete it.</Text>')
          s = s.replace('function TransactionRow({ tx }) { return <View style={styles.transactionRow}>', 'function TransactionRow({ tx, onDelete }) { return <View style={styles.transactionRow}>')
          s = s.replace("<Text style={tx.type === 'income' ? styles.income : styles.expense}>{tx.type === 'income' ? '+' : '-'}{money(tx.amount)}</Text></View>; }", "<View style={styles.txRight}><Text style={tx.type === 'income' ? styles.income : styles.expense}>{tx.type === 'income' ? '+' : '-'}{money(tx.amount)}</Text>{onDelete && <Pressable hitSlop={8} onPress={onDelete} style={styles.deleteButton}><Trash2 size={17} color={colors.red} /></Pressable>}</View></View>; }")
          s = s.replace("function Statistics() {\n  const [txs, setTxs] = useState([]);", "function Statistics() {\n  const navigation = useNavigation();\n  const [txs, setTxs] = useState([]);")
          old = """  useEffect(() => { (async () => { const [t, c] = await Promise.all([getTransactions(), getCategories()]); setTxs(t); setCats(c); })(); }, []);"""
          new = """  const load = async () => {\n    try { const [t, c] = await Promise.all([getTransactions(), getCategories()]); setTxs(t); setCats(c); }\n    catch (error) { Alert.alert('Load failed', 'Could not load statistics. Please try again.'); }\n  };\n  useEffect(() => { const unsubscribe = navigation.addListener('focus', load); load(); return unsubscribe; }, [navigation]);"""
          s = s.replace(old, new)
          s = s.replace("  const save = async () => { await setMonthlyBudget(Number(budget) || 0); setSaved(true); setTimeout(() => setSaved(false), 1600); };", "  const save = async () => { try { await setMonthlyBudget(Number(budget) || 0); setSaved(true); setTimeout(() => setSaved(false), 1600); } catch (error) { Alert.alert('Save failed', 'Could not save the budget. Please try again.'); } };")
          s = s.replace("  noteInput: {", "  txRight: { alignItems: 'flex-end', justifyContent: 'center', gap: 8, marginLeft: 8 },\n  deleteButton: { width: 38, height: 38, borderRadius: 12, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.redSoft },\n  disabledBtn: { opacity: 0.65 },\n  noteInput: {")
          p.write_text(s)
          PY
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx
          git diff --cached --quiet || git commit -m "fix: app functions"
          git push
